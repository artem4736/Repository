Структура файлу:
# Backend: Основи

## 🔹 Вступ
Backend для Smart Reminder. Реалізовано RESTful API на Flask.

## 🔹 Ендпоінти
- `GET /reminders` — отримати список нагадувань  
- `POST /users/register` — створити нового користувача

## 🔹 Приклад запиту
POST /users/register
```json
{"name": "Artem", "email": "artem@email.com"}

Тестування
Перевірено у Postman — запити повертають коректні відповіді.
